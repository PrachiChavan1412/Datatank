in:
    type: file
    path_prefix: /Users/prachichavan/Desktop/Chatbot/data.csv    # path of *.csv or *.tsv file on your local machine
out:
    type: td
    apikey: 10419/3c0963c6b2dce58101f54303b283ac837896077f
    endpoint: api.treasuredata.com
    database: demo_schema
    table: chatbot
    time_column: time
    mode: replace
    #by default mode: append is used, if not defined. Imported records are appended to the target table with this mode.
    #mode: replace, replaces existing target table
    default_timestamp_format: '%Y-%m-%d %H:%M:%S'